# sample shop

Tiny repo with two seeded bugs used by the agent-fleet demo:
`apply_discount` returns the discount instead of the discounted price, and
`slugify` does not collapse or strip separators. Run `python -m unittest -v`.
