{
  "planner": [
    [{"name": "repo_map", "input": {}}],
    [{"name": "finish", "input": {"summary": "median ignores the even-length case.", "subtasks": [
      {"title": "Average the two middle values", "description": "In stats.py, median() returns s[mid] for every length. For even lengths it must return the mean of s[mid - 1] and s[mid]."}]}}]
  ],
  "tester": [
    [{"name": "repo_map", "input": {}}],
    [{"name": "write_file", "input": {"path": "tests/test_regression_median.py", "content": "import unittest\n\nfrom stats import median\n\n\nclass EvenLengthMedian(unittest.TestCase):\n    def test_averages_the_two_middle_values(self):\n        self.assertEqual(median([4, 1, 3, 2]), 2.5)\n\n\nif __name__ == \"__main__\":\n    unittest.main()\n"}}],
    [{"name": "run_tests", "input": {}}],
    [{"name": "finish", "input": {"summary": "median([4, 1, 3, 2]) must be 2.5 (mean of the two middle values).", "test_files": ["tests/test_regression_median.py"]}}]
  ],
  "worker": [
    [{"name": "read_file", "input": {"path": "stats.py"}}],
    [{"name": "apply_patch", "input": {"path": "stats.py", "old": "    mid = len(s) // 2\n    return s[mid]", "new": "    mid = len(s) // 2\n    if len(s) % 2 == 0:\n        return (s[mid - 1] + s[mid]) / 2\n    return s[mid]"}}],
    [{"name": "run_tests", "input": {}}],
    [{"name": "finish", "input": {"summary": "Even-length median averages the two middle values.", "tests_passed": true}}]
  ],
  "reviewer": [[{"name": "finish", "input": {"verdict": "approve", "feedback": "Handles both parities."}}]]
}
